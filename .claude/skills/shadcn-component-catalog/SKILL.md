---
name: shadcn-component-catalog
description: Catalog of available shadcn/ui components in this template, their props, variants, and common composition patterns. Load when composing UI from existing components, writing stories, or deciding which component fits a brief.
---

<!--
TODO(user): populate this skill body with the canonical catalog.

Source of truth: apps/gallery/components/ui/ (34 components installed
from shadcn 4.7's @shadcn/base-nova preset). The gallery's
apps/gallery/lib/components-manifest.ts groups them by category and
includes one-line descriptions.

Recommended sections:
  1. Quick lookup table — slug, default import path (@gallery/ui/<slug>),
     primary use case, one-line variant summary.
  2. Per-component subsection for the heavily-used ones (Button, Card,
     Input, Dialog, Tabs, Table, Form fields). Document CVA variants,
     base-ui render-prop pattern (NOT asChild), and any project-specific
     conventions.
  3. Composition recipes — "form layout", "card + action button",
     "data table with row actions", "split layout", etc. Cross-reference
     the page references in .dsx/page-references/ that demonstrate each.
  4. Out-of-scope — components NOT installed (e.g. Form is missing in
     shadcn 4.7; see KNOWN-ISSUES.md). The agent must NOT invent or
     install new components; surface gaps to the human.

Update cadence: whenever a new component lands in apps/gallery/components/
ui/, regenerate the quick-lookup table.
-->
