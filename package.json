{
  "name": "dsx",
  "version": "0.0.0",
  "private": true,
  "description": "Agent-native design system monorepo (web-first, mobile placeholders).",
  "author": "Bantarus",
  "license": "MIT",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "biome check .",
    "format": "biome format --write .",
    "tokens": "node style-dictionary.config.mjs",
    "design:lint": "design-md lint DESIGN.md",
    "registry:build": "shadcn build registry/registry.json -o apps/web/public/r/",
    "design:sync": "node scripts/design-md-sync.mjs",
    "test:visual": "playwright test",
    "test:visual:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@google/design.md": "latest",
    "@playwright/test": "^1.49.0",
    "style-dictionary": "^4.4.0",
    "turbo": "^2.5.0",
    "typescript": "^5.6.0"
  }
}
