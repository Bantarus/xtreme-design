node_modules
storybook-static
*.log
.turbo
.DS_Store

# Mirror outputs — regenerated by scripts/mirror-storybook-versions.mjs
# via the pipeline. The committed .storybook/versions.json (small,
# deterministic from .dsx/versions/_index.json) is the authoritative
# toolbar list at build/dev time; public/versions/ is larger CSS that
# the decorator fetches at runtime.
public/versions/
public/tokens.active.css
