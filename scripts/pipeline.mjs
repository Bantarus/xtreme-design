#!/usr/bin/env node
// Pipeline stub. Phase 3 fills this in with the real DESIGN.md → gallery
// translation (lint, design-md export, Style Dictionary build, snapshot).
// Phase 1 just makes the Stop hook a no-op so it doesn't bark while the
// rest of the refactor is in flight.

process.stdout.write('pipeline placeholder (Phase 3 will implement)\n');
process.exit(0);
